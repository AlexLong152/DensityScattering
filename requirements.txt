# Python dependencies for the DensityScattering tooling.
# Install with:  pip install -r requirements.txt

# Used by the runtime helpers in tools/package/ (numpy, h5py)
# and by the download / analysis scripts in tools/{,Compton,piphoto,pionpion}/
numpy
h5py
pandas
matplotlib
requests

# Required by tools/DownloadDens.py to query the Jülich density store.
# See https://pypi.org/project/nucdens/
nucdens
